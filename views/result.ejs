<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* Styling for the container box */
        .container-box {
            max-width: 600px;
            margin: 50px auto; /* Center the box horizontally */
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Styling for the heading */
        h2 {
            color: #007bff; /* Bootstrap primary color */
            margin-bottom: 20px;
            text-align: center;
        }

        /* Styling for the details */
        p {
            font-size: 16px;
            line-height: 1.6;
        }

        /* Styling for the list */
        li {
            list-style-type: none;
            padding-left: 0;
            margin-bottom: 10px;
        }

        /* Styling for list items */
        li strong {
            font-weight: bold;
        }

        /* Styling for list item keys */
        li .key {
            font-weight: bold;
            color: #007bff; /* Bootstrap primary color */
        }

        /* Styling for list item values */
        li .value {
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="container-box">
        <h2>Hi <%= name %></h2>
        <p>Here are details of your bank's branch:</p>
        <ul>
            <% Object.keys(result).forEach(key => { %>
                <li><span class="key"><%= key %>:</span> <span class="value"><%= result[key] %></span></li>
            <% }); %>
        </ul>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
